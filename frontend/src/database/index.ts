
// DATABASE DISCONNECTED FOR FRONTEND PREVIEW
// O banco de dados real rodará apenas no servidor Backend (Node.js).
// Este arquivo é apenas um placeholder para não quebrar as importações do frontend.

const sequelize = {
  define: () => {},
  sync: () => {},
  query: () => {},
  close: () => {},
};

export default sequelize;
